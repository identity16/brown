# Personal Cloudflare deployment config — keep your real database_id out of git.
# Copy wrangler.toml.example to wrangler.toml and fill in your own values.
wrangler.toml

# Local secrets used by `wrangler dev`
.dev.vars

# Build / dependency artifacts
node_modules/
.wrangler/
